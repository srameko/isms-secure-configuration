{
  "name": "czechitas-isms-secure-configuration",
  "private": true,
  "scripts": {
    "dev": "slidev",
    "build": "slidev build --base /isms-secure-configuration/ --out dist",
    "preview": "npm run build && npx serve dist -l 4000"
  },
  "dependencies": {
    "@slidev/cli": "^52.12.0",
    "@slidev/theme-default": "^0.25.0"
  },
  "devDependencies": {
    "playwright-chromium": "^1.50.0"
  }
}
